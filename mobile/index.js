import { registerRootComponent } from 'expo';
import App from './App';

// Registers App as the root component. Under Expo this sets up the correct
// entry for both native builds and Expo Go.
registerRootComponent(App);
