{
  "name": "carsign-cloud",
  "version": "0.1.0",
  "private": true,
  "description": "CarSign live map relay — cars publish signs with GPS; a live web map shows everyone's signs and hazards in real time.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "ws": "^8.18.0"
  }
}
